# ppt_deck / visual_director_review

Independently judge whether the current rendered deck realizes its story and visual direction as an audience-facing whole.

Review hierarchy, claim-to-proof composition, deck rhythm, layout variety, readability, visible source fidelity, and whether each page's structural visual actually expresses its rhetorical role. Name weak pages and the smallest coherent rewrite scope. Use current rendered evidence and current context; do not replay a historical defect checklist.

This review supplies an independent director perspective. It may run alongside mechanical screenshot checks; neither review's ordering substitutes for evidence. Export requires fresh rendered-artifact review evidence for the final bytes.

Return the attached `visual_director_review` shape with `director_intent_landed`, `anti_template_ok`, `memory_hook_present`, `homogeneous_layout_risk`, `weak_pages`, `rewrite_action`, and `review_summary`. The host agent owns the judgment; deterministic metrics are supporting evidence only.
