# Superpowers Internal Docs

这里是 `RedCube AI` 的内部 AI / Superpowers 文档区。

当前约定：

- `docs/superpowers/specs/`：本地保留的历史设计冻结稿，不再作为 Git 跟踪公开面
- `guides/`：公开说明与使用文档
- `policies/`：稳定、长期公开保留的规则文档

如果某份内部 spec 已经沉淀成稳定规则，请优先把它提炼到 `policies/` 或 `guides/`，而不是继续扩大内部 spec 的公开职责。
