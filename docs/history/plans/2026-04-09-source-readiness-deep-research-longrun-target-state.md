# Source Readiness Deep Research Longrun Target State

Owner: `RedCube AI`
Purpose: `archived_source_readiness_future_target_reference`
State: `history_provenance`
Machine boundary: 人读历史 source-plane target-state freeze。机器真相继续归 current source contracts、workspace canonical artifacts、CLI/MCP/API 行为、runtime artifacts、owner receipts 和 source owner docs。

日期：`2026-04-09`

本文只保留 source-plane longrun target freeze 的 provenance，不再承担当前 source contract、active plan 或 implementation checklist。当前 source 可执行口径读 `docs/source/source_augmentation_executor_contract.md`、workspace canonical artifacts、runtime-family contracts、source tests 与 runtime-program contracts；当前 RCA 差距与顺序读 `docs/active/rca-ideal-state-gap-plan.md`。

## 历史目标

`Deep Research` 的历史定位是：

- 属于 `Source Readiness`。
- 补事实材料、来源、证据与 readiness。
- 不替代 `Storyline`。
- 不等价于 `MedDeepScientist` 的 `Scout + Idea`。
- 不把 narrative choice 写成 research 结果。

产品语义保持：

`Source Readiness -> Storyline -> Plan -> Visual -> Delivery`

## 历史 Gate Map

| Area | Historical target | Current owner boundary |
| --- | --- | --- |
| Trigger logic | 低信息 topic / keywords、source truth 缺失、blocking source gap 或 pack 未到 `planning_ready` 时触发 augmentation。 | 当前 source contracts、workspace artifacts 和 runtime behavior 决定这条规则是否仍适用。 |
| `planning_ready` | Source audit pass、readiness pack ready、blocking evidence gap resolved。 | 机器真相归当前 source artifacts/contracts/tests。 |
| Residual gap | 只能在显式标注且 non-blocking 时携带。 | 不能隐藏 blocking gaps，也不能让 Storyline/Plan 承担 fact repair。 |
| Durable surfaces | topic canonical source artifacts、request/result/report/readiness artifacts。 | 从 source contracts 和 runtime-family code 验证当前 path/schema。 |
| Family boundary | `ppt_deck`、`xiaohongshu`、guarded `poster_onepager` 共享 substrate；family ontology 保持分开。 | 当前 family contracts 和 active docs 决定 live scope。 |

## 已退役的 active-looking 内容

- The old trigger/gate/surface lists are historical target semantics, not current implementation proof.
- `controller expansion` and `managed web runtime migration` remain explicitly out of scope for this history brief.
- This file cannot prove that current `auditDeliverable`, `runtimeWatch`, `getReviewState` or `getPublicationProjection` agree on source planning gates.

## No-Resurrection 规则

Do not use this file as a source contract, current readiness checklist, prompt-patch recipe or hidden heuristic authorization. Current source-plane changes must be expressed through `docs/source/`, machine-readable contracts, source/runtime code, focused tests, workspace artifacts, owner receipts or typed blockers.
