# 创作阶段 AI-First 审计（2026-04-13）

Owner: `RedCube AI`
Purpose: `historical_ai_first_audit_provenance`
State: `historical_provenance`
Machine boundary: 本文是人读历史审计归档。当前机器真相继续归 contracts、source、CLI/MCP/API 行为、product-entry manifest、runtime artifacts、owner receipts、artifact locator 与 RCA-owned review/export gates。

本文从 `docs/references/` 迁入 `docs/history/plans/`。其中 `upstream Hermes`、创作 owner 表和测试命令只按 2026-04-13 proof/provenance 读取；当前默认 executor、image-first route、OPL-hosted boundary、AI-first quality boundary 和 visual authority truth 回到核心五件套、`docs/policies/ai_first_quality_boundary.md`、`docs/references/rca-visual-deliverable-agent-ideal-state.md`、`docs/active/rca-ideal-state-gap-plan.md`、contracts/source/tests 与 owner receipts。

## 历史审计结果

| Family | Historical AI-first stages | Current read |
| --- | --- | --- |
| `ppt_deck` | storyline、outline、slide blueprint、visual direction、render HTML、director review | 历史 `upstream Hermes` wording 不证明当前 default executor owner；当前 route authority 要从 contracts/source/tests 验证。 |
| `xiaohongshu` | storyline、single-note plan、visual direction、render HTML、director review、publish copy | Source readiness 仍是 fact preparation；当前 creative ownership 归当前 route contracts 与 visual authority surfaces。 |
| `poster_onepager` | storyline、poster blueprint、visual direction、render HTML、director review | Guarded poster scope 继续由当前 contracts 与 active docs 控制。 |
| screenshot review | AI review overlay 加 governed metrics | Mechanical evidence 不能替代 RCA-owned visual judgement 或 review/export verdict。 |
| export routes | packaging，不是 creation | Export success 本身不是 visual ready、handoffable 或 production ready。 |

## 历史修复 Provenance

原审计记录了一轮从 pack/compiler authoring 转向 AI-first structured generation 的历史 tranche，同时把 screenshot mechanics 和 export packaging 保持在 creative-authoring role 之外。

旧 implementation details、exact tests、mock upstream setup 和 environment notes 都只是 historical proof context。当前 test harness、executor backend、route authority 和 image-first route defaults 必须从 live contracts、source、tests 和 active docs 验证。

## 已退役的 active-looking surface

- `upstream Hermes` as a positive current owner label is retired from active docs. Hermes-Agent may be an explicit optional/proof backend or history reference only when current owner surfaces prove that role.
- `pack/compiler` creative authoring resurrection is forbidden unless a current contract/source owner explicitly reopens a non-creative shell role.
- Old test command lists and mock-upstream setup notes are not current verification instructions.
- Environment-dependency backlog wording is not an active plan; current executor/provider defaults belong to current runtime/executor owner docs and contracts.

## No-Resurrection 规则

Do not use this audit to restore Hermes-first default ownership, pack compiler creative builders, mock-upstream-as-default assumptions, hidden fallback chains, old route owner maps, or test commands as current gates. Current AI-first quality and visual-authority claims require current contracts, source/tests, review/export receipts, owner receipts or typed blockers.
