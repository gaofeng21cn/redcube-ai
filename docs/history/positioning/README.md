# 历史定位文档

Owner: `RedCube AI`
Purpose: `historical_positioning_index`
State: `history`
Machine boundary: 人读历史定位索引。机器真相继续归 contracts、source、CLI/MCP/API 行为、product-entry manifest、runtime workspace、review/export gate、artifact locator 与 owner receipts。

本目录保留旧定位词汇和历史边界映射，供 runtime-program 读者上下文和 provenance 检索。这里的 `gateway`、`frontdoor`、`federation`、`Domain Harness OS`、Hermes-first、repo-local runtime pilot 或 workbench 词汇不得读成当前 public identity、active owner、runtime owner 或兼容接口。

当前条目：

- [Domain Harness OS 定位与映射](./domain-harness-os-positioning.md)：`human_doc:domain_harness_os_positioning` 的历史定位语义落点。当前公开身份、runtime owner 和后续差距回到核心五件套、`docs/runtime/`、`docs/active/rca-ideal-state-gap-plan.md` 与 runtime-program contracts。

读取规则：

- `Domain Harness OS`、`Domain Gateway`、`gateway`、`frontdoor`、`federation`、repo-local runtime pilot、old workbench 和 Hermes-first 只作为历史定位词、内部边界词或 semantic-id 读者上下文保留。
- 当前 public identity 是 `RedCube AI` visual-deliverable domain agent；direct path 与 OPL-hosted path 进入 RCA 后回到同一 RCA-owned service-safe domain entry、stage pack、review/export gate 和 artifact authority。
- 当前 OPL boundary 是 stage-led hosted integration / configured family runtime provider；OPL 可以消费 descriptor、domain action refs、receipt refs、operator projection 和 workspace/source/artifact locators，但不能持有 visual truth、review/export verdict、artifact body、memory body 或 owner receipt body。
- 后续若需要引用历史定位中的仍有效规则，应提升到核心五件套、runtime/source/delivery/policy owner docs、active gap plan 或 machine-readable contract；不要把历史 positioning 正文当成 current support doc 扩写。
