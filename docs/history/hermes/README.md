# Repo-Local Hermes Migration History

Owner: `RedCube AI`
Purpose: `historical_hermes_provenance_index`
State: `history`
Machine boundary: 人读历史索引。当前机器真相继续归 contracts、source、CLI/MCP/API behavior、runtime artifacts、owner receipts、核心五件套和 runtime owner docs。

These files are archived provenance from the repo-local Hermes migration line.

They are retained to explain historical tranche decisions, blocker records, and proof closeouts. They do not change the current public capability contract, restore Hermes-first runtime ownership, or prove that upstream `Hermes-Agent` owns RedCube runtime truth.

Current runtime and product-entry truth should be read from:

- `../../project.md`
- `../../status.md`
- `../../architecture.md`
- `../../active/README.md`
- `../../../contracts/runtime-program/current-program.json`

Archived briefs in this directory may still say “current” or “next” from the original cutover date. Read those words only as historical tranche language; current RCA defaults are direct RedCube entry, OPL-hosted provider integration, and `Codex CLI` as the default concrete executor unless an explicit proof backend is selected.
