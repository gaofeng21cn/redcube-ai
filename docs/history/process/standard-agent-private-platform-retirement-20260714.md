# RCA Standard Agent Private-Platform Retirement

Owner: RedCube AI
Purpose: 保存 2026-07-14 私有控制面物理退役的 provenance。
State: history
Machine boundary: 本文不是 callable surface、compatibility contract 或 readiness receipt。

## 退役范围

- `apps/redcube-cli`；
- `packages/redcube-domain-entry`；
- `packages/redcube-runtime`；
- `packages/redcube-runtime-protocol`；
- `packages/redcube-governance`；
- `packages/redcube-overlay-core`；
- current-program index/parts 与私有 product-entry/session/runtime machine baton；
- 对应测试、mock executor、case helper、workspace package dependency 与 CI link path。

## 保留范围

- `agent/` declarative visual pack；
- RCA visual/review/export/artifact/memory/owner authority semantics；
- `python/redcube_ai/native_helpers/`；
- root standard-agent contracts；
- developer proof、native-helper regression 与 no-resurrection guard。

## 目标 owner

OPL Framework 统一持有 package lifecycle、generated surfaces、StageRun、Attempt ledger、session/workspace shell、review/repair transport、native-helper envelope、status/workbench 与 transition materialization。RCA 不保留 fallback implementation。

最终 exact SHA、tests 与 OPL readback 以对应 Git closeout receipt 为准；本文不提前声明 production readiness。
