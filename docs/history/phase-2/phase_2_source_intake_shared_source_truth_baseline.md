# Phase 2 Source Intake + Shared Source Truth Baseline

Owner: `RedCube AI`
Purpose: `historical_phase_2_source_truth_baseline_provenance`
State: `historical_provenance`
Machine boundary: 人读历史 baseline brief。当前机器真相继续归 contracts、source、CLI/MCP/API behavior、workspace artifacts、owner receipts、typed blockers 和当前 source owner docs。

日期锚点：`2026-04-07`

## Lifecycle

本文只保存 source intake / shared source truth 最小 baseline 被吸收进主线的历史 provenance。它不再是当前 operator truth、source contract、formal-entry declaration、后续 tranche board、parking rule 或 current-program owner。

当前 source truth 回到 `docs/source/`、workspace canonical artifacts、runtime-family contracts/source/tests、generated reports、owner receipts 和 typed blockers。

## Historical Fact

这条 baseline 当时把两件事纳入正式主线：

- `source intake`
- `shared source truth`

历史 scope 固定为 canonical source quartet：

- `source-index.json`
- `extracted-materials.json`
- `source-audit.json`
- `source-brief.json`

当时的 baseline 说明 `ppt_deck` 与 `xiaohongshu` 可在同一 substrate 上通过 hydrated contract 消费 shared source truth，并让 review / audit / gate surface 围绕 hydrated truth 而不是 prompt patch 读取。

## Current Owner Read

| Theme | Current owner |
| --- | --- |
| source readiness / augmentation support | `docs/source/README.md`, `docs/source/source_augmentation_executor_contract.md` |
| current RCA completion and gaps | `docs/active/rca-ideal-state-gap-plan.md` |
| current state / architecture / invariants | `docs/status.md`, `docs/architecture.md`, `docs/invariants.md` |

## Historical Content Compression

旧正文中的 formal-entry/readout、family stage consumption lists、后续 tranche 指向和 closeout evidence 清单只按 2026-04-07 历史语境读取。它们不能作为今天的 source implementation plan、current route guide、test lane、formal entry proof 或 readiness evidence。

## No-Resurrection Rule

不要把本文恢复成当前 source contract、shared truth checklist、future tranche board、operator parking rule、formal-entry proof 或 Agent prompt。需要推进 source truth 时，应从 current source owner docs、contracts/source/tests、workspace artifacts、runtime evidence、owner receipts 和 typed blockers 取 live truth。
