# Phase 2 历史记录索引

Owner: `RedCube AI`
Purpose: `historical_phase_2_provenance_index`
State: `history`
Machine boundary: 人读历史索引。当前机器真相继续归 `contracts/runtime-program/current-program.json`、runtime-program leaf parts、source、CLI/MCP/API behavior、workspace/runtime artifacts、owner receipts、核心五件套和当前 owner docs。

生命周期说明：本目录只保留已吸收的 Phase 2 tranche、continuation board、proof lane、manual-test brief 与 closeout provenance。它由旧 `docs/program/phase-2/` 迁入 history；多个 runtime-program contracts 继续通过 `human_doc:program_phase_2_*` 语义 ID 指向这些 brief 的读者上下文。

本目录不表示 Phase 2 仍是新的公开产品方向，也不重新打开 gateway-first、harness-first、Hermes-first、OPL-first runtime、managed web runtime、controller expansion、academic poster 或 new family 主线。当前公开身份、runtime topology、delivery/source truth、review/export authority、native helper boundary 与验证口径以核心五件套、`docs/runtime/`、`docs/delivery/`、`docs/source/`、`docs/policies/` 和 runtime-program contracts 为准。

## 当前读法

- 各 brief 内的“当前状态”“当前结论”“Backlog”“下一步”“停车结论”“closeout”只按原始 tranche 归档时点读取。
- 已吸收 tranche 的命令、测试和 closeout evidence 只证明当时的 hardening/proof lane；它们不能升级为今天的 active plan、implementation checklist、visual ready、exportable、handoffable、domain ready、production ready 或 production visual-stage long-soak 证据。
- 历史手测 brief、activation freeze、source/review/delivery convergence brief、native proof lane 与 HTML closeout 中曾经保留的 CLI 序列、operator flow、测试清单、proof runner 说明、next-tranche pointer 和 closeout 字段流水已压缩为 provenance；当前可执行命令、合同字段、proof runner、source/review/delivery gate 和 route truth 读 source/contracts/tests/owner docs。
- 仍有当前价值的规则必须已经或应当提升到核心五件套、active gap plan、runtime/delivery/source/policy owner docs、machine-readable contracts 或 source/test surface；不要在本目录继续追加活跃计划。

## 历史 brief 读法

| 文档 | 历史角色 | 当前读法 |
| --- | --- | --- |
| `phase_2_source_intake_activation_package_freeze.md` | source intake activation freeze provenance | 只保留 2026-04-07 activation package freeze；不再维护 activation checklist、operator flow、test lane 或 implementation plan。当前 source intake / shared source truth 以 `docs/source/`、canonical workspace artifacts、runtime-program contracts/source/tests、owner receipts 和 typed blockers 为准。 |
| `phase_2_source_intake_shared_source_truth_baseline.md` | shared source truth baseline provenance | 只记录最小 source intake baseline 已吸收；不再维护 formal-entry readout、family stage checklist、next-tranche pointer 或 closeout evidence list。当前 source truth 回到 source docs/contracts/workspace artifacts、owner receipts 和 typed blockers。 |
| `phase_2_review_export_gate_audit_hardening.md` | review/export/gate/audit hardening provenance | 只记录 source readiness gate、review/export truth 和 governance summaries 的 historical hardening；不再维护 current review/export checklist、formal-entry proof 或 test lane。当前 review/export authority 仍归 RCA-owned review/export gates。 |
| `phase_2_family_source_truth_consumption_convergence.md` | stable family source-truth consumption provenance | 只解释 stable family 曾如何收口到 shared source truth；不再维护 source contract table、family route checklist、poster activation rule 或 next-tranche board。当前 source readiness truth 回到 source docs/contracts/workspace artifacts。 |
| `phase_2_publication_projection_delivery_contract_convergence.md` | publication projection / delivery contract provenance | 只保留 delivery contract 与 publication projection convergence 的历史记录；不再维护 delivery schema owner、operator handoff board 或 test command list，也不声明 today 的 handoff/export readiness。 |
| `phase_2_direct_delivery_operator_handoff_hardening.md` | direct-delivery operator handoff provenance | 只记录 direct-delivery handoff hardening；当前 handoff/export truth 回到 delivery owner docs、runtime-program contracts 和 RCA review/export gates。 |
| `phase_2_direct_delivery_lifecycle_stage_convergence.md` | direct-delivery lifecycle-stage provenance | 只保存 lifecycle stage convergence；不把 `xiaohongshu` 改写成 direct-delivery，也不打开 academic poster 或 managed web runtime。 |
| `phase_2_source_readiness_deep_research_trigger_gate_convergence.md` | Source Readiness deep-research trigger/gate provenance | 只说明 Deep Research 被收紧为 Source Readiness 补料能力；它不等同于完整 MDS Scout + Idea，也不重新定义 RCA public identity。 |
| `phase_2_workspace_operator_quickstart_convergence.md` | workspace/operator quickstart provenance | 只记录 quickstart surface hardening；当前 quickstart truth 以 `docs/product/human_quickstart.md`、CLI help、contracts 和 workspace artifacts 为准。 |
| `phase_2_operator_surface_consistency_hardening.md` | operator surface consistency provenance | 只记录 workspace doctor、command help、CLI/MCP watch convergence 的 historical hardening；当前 operator truth 回到 CLI/MCP behavior、runtimeWatch contracts 和 runtime owner docs。 |
| `phase_2_runtime_watch_locator_integrity_hardening.md` | runtimeWatch locator-integrity provenance | 只保留 quartet locator fail-closed hardening；`runtime_watch` 仍不得恢复为 generated `domain_action_adapter` default dispatch。 |
| `phase_2_family_parity_autopilot_continuation_board.md` | historical continuation board | 只保留 Phase 2 family-parity / autopilot continuation board；不是今天的 active backlog 或 public mainline。 |
| `phase_2_family_parity_governance_surface_convergence.md` | family parity governance convergence provenance | 只记录 shared governance surface convergence；当前 stable family route/review/export truth 回到 RCA-owned surfaces 和 runtime-program contracts。 |
| `phase_2_ppt_native_authoring_proof_lane.md` | native PPT selectable-route proof lane | 这是 history 中仍服务当前 optional route support 的 proof brief，但不再维护 proof runner handbook、CI checklist 或 V2 closeout ledger。默认 PPT 路线仍是 image-first；native PPTX 只在用户显式要求可编辑 / DrawingML 时启用，且不绕过 source truth、review、runtimeWatch 或 export gate。 |
| `phase_2_architecture_boundary_governance.md` | historical architecture-boundary governance tranche | 只记录当时的 package/layer/test gate hardening；当前 owner map 与 source-purity tail 以 status、active gap plan、physical morphology policy 和 live source/tests 为准。 |
| `ppt_mainline_quality_closeout.md` | historical HTML lane quality closeout | 只保存 2026-05-01 HTML lane quality debt closeout；不再维护 Plan Closeout、proof transcript 或 route execution plan。当前 `ppt_deck` 默认路线由 image-first production route contracts 持有，HTML/native 都是显式可选路线。 |
| `stable_deliverable_manual_test_brief.md` | historical manual-test brief | 只保留 2026-04-07 stable deliverable hardening 手测 provenance；不再维护 CLI 序列、manual QA rubric、route checklist 或 backlog schema。当前默认入口、运行链路和验证口径回到 status、architecture、contracts、delivery owner docs、runtime evidence 和 review/export gates。 |

## Handling Rules

- 被 `human_doc:*` 引用的 brief 可以迁入 canonical lifecycle 目录；语义 ID 保持稳定。
- 如果某个 brief 后续不再被 runtime-program contract 引用，仍保留在 history 或 tombstone 语境，不回到 active。
- 新增当前主线工作不要继续写进本目录；需要 active tracking 时，在 `docs/active/` 建立新的 owner brief，并同步 machine-readable contract。
- 本目录中的 old gateway / harness / OPL-hosted / Hermes / managed / runtime / domain_action_adapter wording 都按当时 tranche 的历史语境读取，不覆盖当前 owner surface。
