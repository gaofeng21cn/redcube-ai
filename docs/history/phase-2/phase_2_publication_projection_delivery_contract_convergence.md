# Phase 2 Publication Projection Delivery Contract Convergence

Owner: `RedCube AI`
Purpose: `historical_phase_2_publication_projection_provenance`
State: `historical_provenance`
Machine boundary: 人读历史 tranche brief。当前机器真相继续归 contracts、source、CLI/MCP/API behavior、runtime artifacts、owner receipts、typed blockers 和当前 owner docs。

日期锚点：`2026-04-08`

## Lifecycle

本文只保存 publication projection / delivery contract convergence 的历史 provenance。它不再是当前 delivery contract owner、publication projection schema, route checklist, handoff plan, test lane 或 readiness evidence。

当前 delivery / publication truth 回到 `docs/delivery/`、RCA-owned governance surfaces、runtime-program contracts、runtime-family source/tests、workspace artifacts、review/export receipts、owner receipts 和 typed blockers。

## Historical Fact

这条 absorbed tranche 当时收紧了三类历史边界：

- hydrated deliverable contract 暴露 `delivery_contract`。
- `publication-state.json` 作为 topic 级 projection artifact 时，对齐 hydrated `delivery_contract` 和 canonical `review_state`。
- `ppt_deck` / `poster_onepager` direct delivery 与 `xiaohongshu` explicit human publication gate 保持区分。

历史 reader context 中的 key terms 包括 `getPublicationProjection`、`getReviewState`、`required_export_route`、`required_export_bundle_id`、`projection_model`、`human_gate` 和 `delivery_state`。这些语义的当前执行与 gate 继续归 contracts/source/tests、workspace artifacts 和 RCA review/export gates。

## Current Owner Read

| Theme | Current owner |
| --- | --- |
| delivery / publication support | `docs/delivery/README.md`, delivery support docs, route contracts |
| current RCA completion and gaps | `docs/active/rca-ideal-state-gap-plan.md` |
| current state and architecture | `docs/status.md`, `docs/architecture.md` |

## No-Resurrection Rule

不要把本文恢复成当前 delivery schema owner、publication projection guide、operator handoff board、test command list、route readiness proof 或 Agent prompt。需要推进 delivery / publication projection 时，回到 current delivery owner docs、runtime-program contracts、source/tests、workspace artifacts、review/export receipts、owner receipts 和 typed blockers。
