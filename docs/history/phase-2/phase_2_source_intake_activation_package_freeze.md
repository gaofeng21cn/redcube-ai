# Phase 2 Activation Package Freeze

Owner: `RedCube AI`
Purpose: `historical_phase_2_source_intake_activation_provenance`
State: `historical_provenance`
Machine boundary: 人读历史 activation brief。当前机器真相继续归 contracts、source、CLI/MCP/API behavior、workspace artifacts、owner receipts、typed blockers 和当前 source owner docs。

日期锚点：`2026-04-07`

## Lifecycle

本文只保存 `Phase 2 / source intake + shared source truth` activation package freeze 的历史 provenance。它不再是当前 source intake 启动令、operator flow、promotion gate、测试清单、artifact schema owner、implementation plan 或默认停车规则。

当前 source / Phase 2 读法回到这些 owner：

| Theme | Current owner |
| --- | --- |
| RCA 当前完成口径、open gaps、下一轮 baton | `docs/active/rca-ideal-state-gap-plan.md` |
| source readiness / augmentation 支撑 | `docs/source/README.md`, `docs/source/source_augmentation_executor_contract.md` |
| canonical source truth | workspace canonical artifacts, runtime-family contracts/source/tests, generated reports, owner receipts |
| 当前状态与 owner boundary | `docs/status.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md` |

## Historical Fact

这份 freeze 在当时只冻结未来打开 Phase 2 implementation baton 前需要具备的条件、对象边界、canonical artifact 名称、gate surface、operator 读取顺序、最小测试面和 closeout evidence 要求。它明确不直接实现 source intake runtime build-out，不扩大 controller，不新增 family / overlay，不推进 poster academic contract，也不推进 OPL-hosted runtime integration。

后续 `Phase 2 source intake / shared source truth baseline` 已在该 freeze 基础上进入主线；因此本文只按历史 activation freeze closeout 读取。它不能重新成为今天的 Phase 2 implementation checklist，也不能把 2026-04-07 的 promotion 条件写成当前 longrun 停车规则。

## Historical Boundary

旧正文中的 artifact 名称仍可作为历史 reader context 读取：

- `source-index.json`
- `extracted-materials.json`
- `source-audit.json`
- `source-brief.json`

当前这些语义的可执行真相不由本文维护。current owner 是 workspace canonical artifacts、source readiness / augmentation contracts、runtime-family source/tests、generated reports、owner receipts 和 typed blockers。

## Historical Content Compression

旧正文包含激活条件、对象边界、artifact schema 表、gate surface、operator flow、测试列表、closeout evidence 列表和当前停点。它们已压缩为 provenance：

- 历史上它证明 activation package freeze 已完成。
- 当前它只解释后续 source-intake baseline 为什么有这个 predecessor。
- 当前它不授权任何 source runtime build-out、controller expansion、route implementation、workflow promotion、test command selection 或 readiness claim。

## No-Resurrection Rule

不要把本文恢复成：

- 当前 source intake / shared source truth implementation plan。
- 当前 operator flow、promotion gate、activation checklist 或 test lane。
- current-program、status、runtime report、source docs 或 active gap plan 的第二真相源。
- 证明 source planning-ready 之外任何 visual ready、exportable、handoffable、domain ready、production ready 或 long-soak complete 的证据。
- 允许绕过 current source contracts、workspace artifacts、owner receipts、typed blockers 或 RCA review/export gates 的兼容路径。

如果需要重新打开 source intake 方向，应先从当前 source owner docs、contracts/source/tests、workspace artifacts、runtime evidence、owner receipt 或 typed blocker surface 建立新的 owner-delta；本文只提供 2026-04-07 activation freeze 的历史上下文。
